int i = 0, &r1 = i;
    double d = 0, &r2 = d;
    // 正确
    r2 = 3.14159;
    std::cout << r2 << "\n";
    // 正确，发送自动类型转换
    r2 = r1;
    std::cout << r2 << '\n';
    // 正确，发送自动类型转换
    i = r2;
    std::cout << i << std::endl;
    // 正确，发送自动类型转换
    r1 = d;
    std::cout << r1 << std::endl;
